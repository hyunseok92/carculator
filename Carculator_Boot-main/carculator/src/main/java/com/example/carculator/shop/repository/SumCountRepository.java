package com.example.carculator.shop.repository;

import com.example.carculator.shop.entity.SumCount;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SumCountRepository extends JpaRepository<SumCount, String> {
}
